package chapter9.langPackage;

public class Value {
    // 비교를 위해 값을 입력받을 클래스 생성
    // 인스턴스 변수 선언
    int value;

    // 생성자 -> 매개변수 value 를 인스턴스변수 값으로 초기화한다
    Value(int value) {
        this.value = value;
    }
}
