package test.code;

public class Start {
    public static void main(String[] args) throws Exception {
        Game game = new Game();
        game.pin(1);
        game.pin(4);
        game.pin(2);
        game.pin(10);
        game.pin(3);
        game.pin(5);
    }
}
