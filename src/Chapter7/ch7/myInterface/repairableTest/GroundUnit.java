package Chapter7.ch7.myInterface.repairableTest;

public class GroundUnit extends Unit {
    // GroundUnit 기본 생성자 -> 매개변수로 받은 hp는 부모 클래스인 Unit 의 변수 hp 에 초기화 된다
    GroundUnit(int hp) {
        super(hp);
    }
}
