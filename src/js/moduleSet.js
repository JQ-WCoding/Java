const arr = [1, 2, 3];

arr.forEach(console.log);

let userId = 1;
let userName = 'Lee';

let user = {id: 1, name: 'Lee'};

let users = [
    {id: 1, name: 'Lee'}
    , {id: 2, name: 'Kim'},
];

users.forEach(console.log);